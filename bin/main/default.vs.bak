#version 330

layout (location=0) in vec3 inPosition;

uniform mat4 transform;
uniform float aFloat;

out vec4 color;

void main()
{
	color = vec4(clamp(inPosition, 0.0, aFloat), 1.0);
    gl_Position = transform * vec4(inPosition, 1.0);
}